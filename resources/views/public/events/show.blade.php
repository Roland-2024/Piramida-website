<x-layouts.public :title="$translation->seo_title ?: $translation->title" :description="$translation->seo_description ?: $translation->short_description" :language-urls="$languageUrls">
    <article class="mx-auto max-w-4xl px-5 py-16 lg:px-8"><p class="text-sm font-semibold text-amber-700">{{ $event->starts_at->format('d M Y · H:i') }} – {{ $event->ends_at->format('d M Y · H:i') }}</p><h1 class="mt-4 text-4xl font-semibold tracking-tight sm:text-5xl">{{ $translation->title }}</h1><p class="mt-4 text-lg text-slate-600">{{ $translation->location }}</p>@if ($event->featuredMedia)<img src="{{ $event->featuredMedia->url() }}" alt="" class="mt-9 max-h-[32rem] w-full rounded-3xl object-cover">@endif<div class="prose-content mt-10 text-slate-700">{!! $translation->description !!}</div>@if ($event->external_url)<a href="{{ $event->external_url }}" target="_blank" rel="noopener" class="mt-8 inline-block rounded-lg bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Register / more information</a>@endif</article>
</x-layouts.public>
