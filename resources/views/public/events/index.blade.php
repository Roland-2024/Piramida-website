<x-layouts.public :title="__('cms.events')" :language-urls="$languageUrls">
    <section class="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div class="flex flex-wrap items-end justify-between gap-4"><h1 class="text-4xl font-semibold">{{ __('cms.events') }}</h1><div class="flex rounded-lg border border-slate-200 bg-white p-1 text-sm"><a href="{{ route('public.events.index', app()->getLocale()) }}" class="rounded-md px-3 py-2 {{ $period === 'upcoming' ? 'bg-slate-950 text-white' : '' }}">{{ __('cms.upcoming') }}</a><a href="{{ route('public.events.index', [app()->getLocale(), 'period' => 'past']) }}" class="rounded-md px-3 py-2 {{ $period === 'past' ? 'bg-slate-950 text-white' : '' }}">{{ __('cms.past') }}</a></div></div><div class="mt-9 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse ($events as $event) @php $item = $event->translation(app()->getLocale()); @endphp<article class="overflow-hidden rounded-2xl border border-slate-200 bg-white">@if ($event->featuredMedia)<img src="{{ $event->featuredMedia->url() }}" alt="{{ app()->getLocale() === 'en' ? $event->featuredMedia->alt_text_en : $event->featuredMedia->alt_text_al }}" class="h-64 w-full object-cover">@endif<div class="p-5"><p class="text-xs font-semibold uppercase text-amber-700">{{ $event->starts_at->format('d M Y · H:i') }}</p><h2 class="mt-3 text-xl font-semibold">{{ $item?->title }}</h2><p class="mt-2 text-sm text-slate-500">{{ $item?->location }}</p><p class="mt-3 text-sm text-slate-600">{{ $item?->short_description }}</p><a href="{{ route('public.events.show', [app()->getLocale(), $item?->slug]) }}" class="mt-5 inline-block text-sm font-semibold text-amber-700">{{ __('cms.read_more') }}</a></div></article>@empty<p class="text-slate-500">{{ __('cms.no_content') }}</p>@endforelse</div><div class="mt-8">{{ $events->links() }}</div></section>
</x-layouts.public>
