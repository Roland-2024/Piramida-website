<x-layouts.public :title="__('cms.news')" :language-urls="$languageUrls">
    <section class="mx-auto max-w-7xl px-5 py-16 lg:px-8"><h1 class="text-4xl font-semibold">{{ __('cms.news') }}</h1><div class="mt-9 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse ($articles as $article) @php $item = $article->translation(app()->getLocale()); @endphp<article class="overflow-hidden rounded-2xl border border-slate-200 bg-white">@if ($article->featuredMedia)<img src="{{ $article->featuredMedia->url() }}" alt="" class="h-52 w-full object-cover">@endif<div class="p-5"><p class="text-xs text-slate-500">{{ $article->published_at->format('d M Y') }}</p><h2 class="mt-3 text-xl font-semibold">{{ $item?->title }}</h2><p class="mt-3 text-sm text-slate-600">{{ $item?->excerpt }}</p><a href="{{ route('public.news.show', [app()->getLocale(), $item?->slug]) }}" class="mt-5 inline-block text-sm font-semibold text-amber-700">{{ __('cms.read_more') }}</a></div></article>@empty<p class="text-slate-500">{{ __('cms.no_content') }}</p>@endforelse</div><div class="mt-8">{{ $articles->links() }}</div></section>
</x-layouts.public>
